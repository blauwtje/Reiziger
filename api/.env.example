# Copy to api/.env (optional). Defaults shown; all are optional for local dev.
# PORT=3001
# HOST=127.0.0.1
# OTP_URL=http://localhost:8080
# OTP_GRAPHQL_PATH=/otp/gtfs/v1
# FEED_ID=ovapi
# Paths default to the repo's otp/data and api/data; override only if you move things.
# GTFS_DIR=
# TRANSFERS_ORIG=
# RULES_PATH=
